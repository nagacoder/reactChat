import React from "react";
import { View, Text, Image } from "react-native";

const ChatSection = () => {
  return (
    <View
      style={{
        flexDirection: "row"
      }}
    >
      
    </View>
  );
};

export default ChatSection;
